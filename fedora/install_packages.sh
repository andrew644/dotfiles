#!/usr/bin/env bash

sudo dnf do \
	--action=install neovim \
	--action=install kitty \
	--action=install cascadia-mono-nf-fonts \
	--action=install htop \
	--action=install zsh \
	--action=install fastfetch \
	--action=install audacity \
	--action=install mpv \
	--action=install vlc \
	--action=install yt-dlp \
	--action=install ffmpeg \
	--action=install ffmpeg \
	--action=install zathura \
	--action=install gimp \
	--action=install inkscape \
	--action=install dotnet-runtime-10.0 \
	--action=install dotnet-sdk-10.0 \
	--action=install mono-core \
